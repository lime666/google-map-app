<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Map App</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <%= link_to 'Admins', admin_users_path, class: 'nav-link'%>
          </li>
          <li class="nav-item">
            <%= link_to 'Location', admin_locations_path, class: 'nav-link'%>
          </li>
          <li class="nav-item">
            <%= link_to 'Dashboard', admin_root_path, class: 'nav-link'%>
          </li>
        </ul>
          <form class="d-flex">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <% if !admin_user_signed_in? %>
              <li class="nav-item">
                <%= link_to 'Sign in', admin_user_session_path, class: 'nav-link', method: :post %>
              </li>
              <% end %>
              <% if current_admin_user %>
                <li class="nav-item">
                  <%= link_to 'Log out', destroy_admin_user_session_path, class: 'nav-link', method: :delete %>
                </li>
              <% end %>
            </ul>
          </form>
        </ul>
      </div>
  </div>
</nav>
