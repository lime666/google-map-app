<p style="color: green"><%= notice %></p>

<h1>Locations</h1>

<div id="locations">
  <% @locations.each do |location| %>
    <%= render location %>
    <p>
      <%= link_to "Show this location", admin_location_path(location) %>
    </p>
  <% end %>
</div>

<%= link_to "New location", new_admin_location_path %>
